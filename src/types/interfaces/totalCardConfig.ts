// NOTE:logo (string) - the path for localStorage.png;
// NOTE: the Array of background colors from enum;

export interface CardConfig {
  logo?: string
  amount: number
  title: string
  bgColor?: string
}
